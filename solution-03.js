{
    /**
     * This solution works for challenges: none yet
     */
	init (elevators, floors) {
		elevators.forEach(elevator => {
			// Handles where to stop after passenger press floor buttons
		});

		floors.forEach((floor, floorNum) => {
			// Everything should start from here
		});
	},
    update (dt, elevators, floors) {
        // Silence is golden
	},
}
